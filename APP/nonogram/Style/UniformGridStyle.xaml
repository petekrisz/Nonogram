<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style TargetType="TextBlock" x:Key="UniformTextBlockStyle">
        <Setter Property="TextAlignment" Value="Center" />
        <Setter Property="FontSize" Value="{Binding DataContext.FontSize, RelativeSource={RelativeSource AncestorType=UserControl}}" />
        <Setter Property="Width" Value="{Binding DataContext.CellSize, RelativeSource={RelativeSource AncestorType=UserControl}}" />
        <Setter Property="Height" Value="{Binding DataContext.CellSize, RelativeSource={RelativeSource AncestorType=UserControl}}" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="HorizontalAlignment" Value="Center" />
    </Style>

</ResourceDictionary>